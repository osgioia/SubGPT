[tool.poetry]
name = "subgpt"
version = "0.1.0"
description = ""
authors = ["Osvaldo Gioia <os.gioia@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
openai = "^0.28.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
